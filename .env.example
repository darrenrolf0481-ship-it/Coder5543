# .env.example
# Server-side only — never exposed to the browser bundle
GITHUB_TOKEN=
# Ollama host used by the backend proxy when the frontend does not send one
OLLAMA_HOST=http://127.0.0.1:11434
# Client-side (baked into Vite bundle) — restrict to localhost for development
VITE_GEMINI_API_KEY=
